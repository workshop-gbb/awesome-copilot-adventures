function refine(initialValue, step, maxIterations) {
  // TODO: implement the bounded stabilization loop.
  return { value: initialValue, iterations: 0, stable: false };
}

module.exports = { refine };
